databaseChangeLog:
  - changeSet:
      id: 000-create-schema
      author: codex
      preConditions:
        - onFail: CONTINUE
        - onError: CONTINUE
        - sqlCheck:
            expectedResult: 0
            sql: "SELECT COUNT(*) FROM information_schema.schemata WHERE schema_name = 'algo'"
      changes:
        - sql: "CREATE SCHEMA IF NOT EXISTS algo"
